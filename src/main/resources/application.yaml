spring:
  application:
    name: oredata
  datasource:
    url: jdbc:h2:file:./demo
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update

